<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ApplicationThemes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/green</item>
        <item name="colorSecondary">@color/greenLight</item>
    </style>

    <style name="Base.Theme.ActionBarTheme" parent="Theme.Material3.Light">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorSecondary">@color/greenLight</item>
    </style>

    <style name="Theme.ApplicationThemes" parent="Base.Theme.ApplicationThemes" />
</resources>